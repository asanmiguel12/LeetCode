public class gcd {

    /** For two strings s and t, we say "t divides s" if and only if
     s = t + t + t + ... + t + t (i.e., t is concatenated with itself one or more times).
     Given two strings str1 and str2, return the largest string
     x such that x divides both str1 and str2.
     */

    public static String gcd(String str1, String str2) {
        char [] gcd = str1.toCharArray();

        for (char i = 1; i <= str1.length(); i++) {
            System.out.println(i);

        }

    return null;
    }
}
